# requirements.in

# For YAML parsing
PyYAML

# For making HTTP requests (used in earlier versions, may still be needed)
requests

# For .gitignore pattern matching (used in earlier versions, may still be needed)
pathspec

# For nice looking CLI tables
tabulate

# Standard library modules used:
# os
# subprocess
# sys
# logging
# platform
# typing
